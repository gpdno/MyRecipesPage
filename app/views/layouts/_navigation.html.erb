<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a <%= link_to 'Our Family Recipes', root_path, class: "navbar-brand" %>></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <li class="active"><a <%= link_to "Recipies", recipes_path %></a></li>
          <li><a <%= link_to "New Recipe", new_recipe_path %></a></li>
        <% end %>
        <li><a <%= link_to "Link #2", "" %></a></li>
        <% if logged_in? %>
          <li><a <%= link_to "Logout", logout_path %></a></li>
        <% else %>
          <li class="active"><a <%= link_to "Login", login_path %></a></li>
        <% end %>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.chefname %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a <%= link_to "View Profile", chef_path(current_user) %></a></li>
              <li><a <%= link_to "Edit Profile", edit_chef_path(current_user) %></a></li>
              <li role="separator" class="divider"></li>
              <li><a <%= link_to "Link #2", "" %></a></li>
              <li role="separator" class="divider"></li>
              <li><a <%= link_to "Logout", logout_path %></a></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
